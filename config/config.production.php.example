<?php
/**
 * Production Configuration File
 * 
 * INSTRUCTIONS:
 * 1. Copy this file to config.php
 * 2. Fill in your hosting provider's database credentials
 * 3. Update SITE_URL to your actual domain
 * 4. Keep this file secure - never commit it to public repositories
 */

// Database Configuration
// Get these from your hosting provider's cPanel or database panel
define('DB_HOST', 'localhost'); // Usually 'localhost' on shared hosting
define('DB_USER', 'your_database_username'); // Your MySQL username
define('DB_PASS', 'your_strong_database_password'); // Your MySQL password
define('DB_NAME', 'your_database_name'); // Your database name (e.g., username_blog)

// Site Configuration
define('SITE_NAME', 'KSA News');
define('SITE_URL', 'https://yourdomain.com'); // CHANGE THIS to your actual domain
define('SITE_LOGO', 'logo.png'); // Set to false or empty string to use text only

// File Upload Configuration
define('UPLOAD_DIR', __DIR__ . '/../assets/images/');
define('UPLOAD_MAX_SIZE', 5242880); // 5MB in bytes

// Session Configuration
ini_set('session.cookie_httponly', 1);
ini_set('session.use_only_cookies', 1);
session_start();

// Error Reporting - CRITICAL: Set to 0 in production for security!
error_reporting(0);
ini_set('display_errors', 0);

// Optional: Log errors to a file instead
// ini_set('log_errors', 1);
// ini_set('error_log', __DIR__ . '/../logs/php-errors.log');
?>



